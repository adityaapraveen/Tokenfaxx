{
  "id": "fix-notification-migration",
  "description": "Fix the failing notification settings migration",
  "repository": ".",
  "startingCommit": "HEAD",
  "timeoutMs": 900000,
  "setup": "pnpm install --frozen-lockfile",
  "validation": { "test": "pnpm test", "typecheck": "pnpm typecheck" },
  "expectedOutcome": { "testsPass": true, "typecheckPasses": true },
  "maximumCostUsd": 5,
  "tags": ["migration"]
}
